Name = {{ ansible_hostname | regex_replace('-', '') }}
AddressFamily = ipv4
Interface = tun0
{% for host in groups['all'] %}
{% if ansible_hostname != hostvars[host]['ansible_hostname'] %}
ConnectTo = {{ hostvars[host]['ansible_hostname'] | regex_replace('-', '') }}
{% endif %}
{% endfor %}
